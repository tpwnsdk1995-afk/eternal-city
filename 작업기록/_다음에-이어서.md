# 다음에 이어서 — 원작 그림 연결 (2026-09-16 밤 시작, 사용자 취침 중 자율 진행)

## 원칙
- 질문 금지, 자율 진행. 어설프면 보여주지 말고 다시 (원작 스크린샷 docs/ref/*.jpg 와 비교해 진짜 같아야 함).
- 단계마다 `npx tsc --noEmit && npx vitest run && npm run build` → 커밋 → push (origin = https://github.com/tpwnsdk1995-afk/eternal-city).
- 모델 순서: **Fable이 필요한 것(역공학·그림 판단) 먼저**, Opus로 충분한 것은 맨 뒤. Fable 한도 걸리면 6번부터 Opus로.
- 원본 클라이언트(D:\이터널시티)는 레포에 넣지 않는다. public/art/ 에 필요한 PNG만.

## 순서 / 상태
⚠ 2026-09-16 밤 사용자 지시: **6번(M14 잔여)을 먼저 끝내고** 3→4→5(원작 그림) 순서로 진행. 원작 그림은 "원본처럼 보여야" 하는 기준이라 시간이 제일 오래 걸리므로 뒤로 미룸.
1. [Fable] 중곡동 기본 좀비 `zombie_casual_f` 원본 찾기 — 전체 .dat 첫 프레임 썸네일 스캔(scratchpad/thumbs) → 눈으로 선별 ☑ **05020.dat rid 13500** (같은 묶음 13501~13504 = 뚱보넥타이·초록셔츠·대머리흰셔츠·양복 좀비)
2. [Fable] 추출기(tools/) → public/art/zombie_casual_f.png (8행=Dir순서 × 6열) → ART_OVERRIDES → 프레임 크기 조정 → 게임 스크린샷 vs docs/ref 비교 ☑ (e2e/out/zombie_shot1.png, 커밋 2단계)
3. [Fable] 나머지 캐릭터 같은 파이프라인: 중곡동 좀비 5종(suit_m·stripe·banshee·office·worker) ☑ → 플레이어 ☑(00046.dat 5478, 무기 오버레이는 생략) → NPC ☑(11종, 03xxx/04xxx·00012·00061 정지 포즈) → 다른 몬스터 ☑(32종)
4. [Fable] 바닥 타일 — .btx(DDS) 해독 → 원작 스와치 5종(아스팔트·보도·콘크리트·흙·잔디) tools/ec-tiles.py → draw/tiles.ts 바탕층 분기 ☑ (지붕·벽·상점·차·하수도·물·철로는 원작에 탑뷰 소스 없어 그린 버전 유지)
5. [Fable] UI — ui_panel/ui_button 9-slice 오버라이드 ☑ (tools/ec-ui.py: 00178.dat 17625 MINI MAP 프레임 빗금 밴드+은색 테두리 → ui_panel 256×48, 00134.dat 20062 블록1 닫기 버튼 → ui_button 20×20; ui_slot/ui_divider는 원작 소스 없어 그린 버전 유지)
6. [Opus OK] M14 잔여: (b) 총기 아이콘 밝기 보정 ☑ (c) 어설트 S등급 업적 2종 ☑ (d) 보관함 분류탭/정렬(Window.filterSortBar 공용) ☑ README M14 ☑ e2e 전체(Edge) ☑ 커밋/푸시 ☑ 아티팩트 v26 ☑ → **5번 완료(UI), 6번 아이콘 22종 완료 → 효과음 37개 자동 배정 완료, GitHub Pages 배포 완료, 휴대폰 터치 전체화면 수정(b0a6288)**

## 원작 포맷 메모 (해독 완료, scratchpad/ecdec.py)
- .dat/.dta = 스프라이트 컨테이너: u8 0, u16 count, count×(u32 rid, u32 off), blobs
- blob: u32 L, u32 X(픽셀 섹션 시작), u16 A(파트수), u16 B, u16 0, (A-1)×u32 파트 오프셋
- part: u16 C(블록수=방향 8 등), u16 D, u16 0, (C-1)×u32 블록 오프셋 / block: u16 n, n×u32 프레임 오프셋
- frame: u16 nlayers, nlayers×(u32 ptr, u32 pixoff) / layer: u8 type, u16 w, u16 h, s16 ox, s16 oy, h행×[u16 n][n×(skip,run)]
- 픽셀: RGB565 리틀엔디안, X + pixoff(바이트). 확인: 00012.dat id 4305 = 남자 기본 몸 8방향×132프레임 26×62.
- 파일 번호대(썸네일 스캔 기준): 03xxx=아이템 아이콘, 04/05/06xxx=캐릭터·몬스터 스프라이트, 07~09xxx=소품/구조물. .btx=19바이트 헤더 뒤 DDS(DXT1) 지형 텍스처 묶음, s0/s1 .dat=그냥 WAV 효과음, .mak/.flg=파일 1개짜리 ZIP(맵 격자/플래그), .bcd=커서 추정, ab .dat/.bgd/.bgi=미해독. 사용자 승인: 전부 참고해서 써도 됨(2026-09-16).

## 추출 파이프라인 (tools/ec-extract.py)
- `python tools/ec-extract.py <key> --strip` → tools/out/<key>_strip.png (정면 블록 전 프레임, 번호 표시) → 눈으로 idle/walk/aim/death 프레임 고름
- SPRITES[key] = file, rid, parts(A2 몬스터는 part0=스프라이트, part1=큰 미리보기 → [0]만), frames
- `python tools/ec-extract.py <key>` → public/art/<key>.png, 셀 크기는 자동(사용 프레임 최대 범위) → 출력된 frameW/frameH를 artOverrides.ts 에 적용
- 게임 쪽: ART_OVERRIDES 시트에 `scale`(원본 62px 인물을 48px 그림 인물 크기에 맞춤 = 0.6), Enemy 는 artScale(def.tex) 곱함, 히트 원은 this.height 기준
- zombie_casual_f 프레임 지도(40프레임/방향): 0~7 걷기, 8~13 공격, 14~15 ?, 16~28 대기, 29~34 죽음(34 누움), 35~39 피격
- 스크린샷: scratchpad/zz-shot.spec.ts + zz-shot.config.ts 를 e2e/ 로 복사 → `npx playwright test -c e2e/zz-shot.config.ts zz-shot.spec.ts` (설치된 Edge 사용, playwright 브라우저 다운로드 불필요) → e2e/out/*.png. **끝나면 다시 e2e/ 에서 빼둘 것**(npm run e2e 가 집어감).
- 도구 주의: Bash 도구 heredoc 안에서는 백슬래시가 한 단계 접혀 파이썬 문자열 패치가 깨진다 → 백슬래시 있는 수정은 Write/Edit 도구로. Git Bash에 strings 없음.

## 진행 로그
- 2026-09-16 밤: 포맷 해독 완료, git 연결, 2시간 크론 예약, 썸네일 스캔 시작.
- 2026-09-16 20:45: zombie_casual_f 원작 그림으로 교체 완료(tsc·vitest 224·build 통과, 스크린샷 확인). 다음: 3번 나머지 캐릭터.
- 2026-09-16 밤(Fable, 컨텍스트 압축 후 재개): M14 (b)(c)(d)+README 완료, 파비콘 추가(Edge favicon 404 해소). 보관함 e2e 1번 원인 = Esc가 다음 프레임 closeTop()으로 처리되는데 직후 openWindow('storage')가 먼저 열려 보관함이 닫힘 → 스펙에 대화창 닫힘 대기 1줄. tsc·vitest 225 통과. 남은 것: e2e 전체(Edge) → build → 커밋/푸시 → 아티팩트 v26 → 3번(좀비 5종: zombie_suit_m 시트는 만들어져 있으나 ART_OVERRIDES 미연결, 나머지 4종 프레임 지도만).
- 2026-09-16 밤(계속): e2e 전체 Edge 55/56 → 감염체 스펙 실패 원인 = 발톱 전투의 발사 쿨다운이 무기 공용(gameState.fire)이라 산성 토사(rpm 40=1.5s) 첫 발이 늦게 나가 좀비 뒤에 착탄 → 스펙에 1.6s 대기 1줄. 재실행 감염체·보관함 8/8 통과. build 통과, M14 커밋/푸시. 다음: 아티팩트 v26 → 사용자에게 "디자인 들어간다" 알림 → 3번 좀비 5종.
- 2026-09-16 밤(디자인 시작): 사용자에게 "M14 끝, 디자인 들어감" 알림(푸시). 추출기 auto_cell에 FEET=36 고정(발이 셀 중앙 +36px) → 6종 시트 재추출, ART_OVERRIDES 6종(scale 0.6). 스크린샷 e2e/out/zombie6_shot1.png 확인: 6종 정렬·크기 일치. tsc·vitest 225·build 통과. 다음: 플레이어 원작 스프라이트(00012.dat id 4305 남자 기본 몸 26×62 ×132프레임 — 무기별 자세 확인 필요).
- 2026-09-16 밤(플레이어): 00012 4305 대신 조합형 남자 모델 00046.dat 5478(파트 0 몸·4 은발·5 청재킷·6 청바지·17 운동화) 채택. 151프레임 지도: 0~11 대기, 12~23 걷기, 36~41 뒤로 쓰러짐, 52~59 달리기, 72~75 한손(권총) 발사, 76~90 양손(장총) 발사, 24~25 팔 휘두름, 143~150 오토바이. 머리 파트 0번 방향 12프레임이 깨져(레이어 수 쓰레기값) 추출기에 sane_layer 가드 추가 + 걷기 14·18·22 사용. 시트 3장(player 권총 74 / player_long 장총 80 — smg·rifle·shotgun·sniper·mg·launcher 공용 / player_melee 25), cell 합집합으로 150×156 통일. Player.ts: look=artScale(tex)로 setScale 3곳 곱하고 몸통 원을 refreshWeaponLook에서 r=10/look로 재설정. 무기 파트(7·9·14·16, 3~5px)는 생략. 스크린샷 e2e/out/player_*.png 확인(좀비와 크기 일치). tsc·vitest 225·build 통과, e2e 전체는 실행 중. 다음: NPC(00136~00198 2파트 인물) → 다른 몬스터(개 00021/00022, 군인 00060/00061).
- 2026-09-16 밤(NPC): e2e 56/56 통과 후 플레이어 커밋 287da23 푸시. NPC 11종 배정: 엘리아 03960 6593 · 무기상 03880 6296(앞치마) · 기술상 03990 6635 · 암거래상 03950 6579 · 보관소 04056 17094 · 구청 과장 03770 6122 · 패러렐 04070 6687(흰 정장) · 사이버샵 04062 17536 · 김훈 00061 5482(위장복, 03780은 앉아 쏘는 자세라 제외) · 운송조합원 04040 6664 · 교관/접수원 00012 4305(파트 0·2·1). Npc.ts가 S열 대기 프레임만 쓰므로 6열 전부 프레임 0(시트 20~45KB). Npc.ts: setScale(artScale)+이름표 y=-10-displayHeight/2. 원작 인물 키 68~86px로 플레이어 71px과 같은 배율 0.6. 스크린샷 e2e/out/npc_hub/junggok/achasan.png 확인. 다음: 다른 몬스터(개 00021/00022, 군인 00060, 03810/03830/03850 좀비, 03790/04060 거미, 04020 헐크, 04080 오거, 04050).
- 2026-09-16 밤(몬스터): 32종 배정 완료 — 파일:rid·프레임 지도(대기/걷기3/공격/죽음)는 tools/ec-extract.py 항목 주석. 원작 시트가 자기 비율을 갖고 있어 Enemy.ts는 def.scale 대신 artScale(0.6)로 setScale, 이름표/HP바는 대기 머리 높이 top(추출기가 출력, artOverrides의 top 필드)에 걸음(셀 높이는 공격 프레임 기준이라 셀 위쪽에 걸면 보스 이름표가 붕 뜸). 스크린샷 e2e/out/mon_0~7.png 확인(32종 전부 크기·이름표 정상). tsc·vitest 225·e2e 56(Edge)·build 통과, 커밋 edbd03d 푸시, 아티팩트 v30(art png 32장 추가, 총 ~13MB). 같은 커밋에 전체화면 토글(제목 화면 버튼+F, Esc 메뉴 버튼, 터치 탭 ⛶는 원래 있음; scale.toggleFullscreen). 라바(05650:15511)는 큰 흰 가시 인간형인데 bodyRadius 9라 나중에 재검토 여지. 다음: 4번 타일 — .btx(DDS DXT1) 지형 텍스처 → draw/tiles.ts drawTilesAtlas 분기.
- 2026-09-17 새벽(타일): 원작 지형은 512×256 다이메트릭 슬라브(00016/00017 .btx, 우리 32px 격자의 2.5~3배 축척)라 좌표계 변환 대신 재질 스와치만 채택 — tools/ec-tiles.py가 00016 #36(아스팔트 차선 사이 80px)·#10(보도 64px=포장돌 정수배, 96px은 모아레)·#10(콘크리트)·#17(흙 128px, 96px은 능선 반복)·#14(잔디)를 32px로 축소 + roll-blend로 이음새 제거 → public/art/tiles.png(160×32). TextureGenScene이 로드해 setTileSwatches, asphalt/sidewalk/sidewalkBlock/grass/dirt/parkingFloor(콘크리트+어둡게)가 바탕층으로 그림, 없으면 절차 그리기 그대로. 스크린샷 e2e/out/tiles_safezone/street/park.png 확인. tsc·vitest 225·e2e 56(Edge)·build 통과, 커밋 푸시, 아티팩트 v31(art/tiles.png 추가). 한계: 재질당 스와치 1장이라 넓은 아스팔트에서 32px 주기 보임 → 필요하면 TilemapBuilder에서 셀별 변형 선택. 크롬 뷰어 안 전체화면 버튼은 효과 없음(iframe) → 브라우저 새 탭/F11/홈 화면 추가 안내 필요. 다음: 5번 UI 9-slice(ui_panel/ui_slot) → 아이템 아이콘(03xxx) → 사운드(s0/s1 WAV).
- 2026-09-17 새벽(UI): 원작 UI 리소스 00114~00200 접촉 시트 스캔 → 창 내부는 투명(프레임만)이라 목탄 배경을 PNG에 굽고 상단 8px 빗금 밴드/2px 은색 테두리/모서리 사선만 원작에서 잘라 ui_panel 9-slice(8px), 닫기 버튼 4모서리로 ui_button(3px). UIScene HUD 바 여백 6→8. tsc·vitest 225·build 통과, e2e/out/ui_chrome.png(HUD+상태창+인벤토리) 확인. 다음: 6번 아이템 아이콘(03xxx .dat, 00102~00113·00122/00123 대형 무기 아이콘)·효과음.
- 2026-09-17 새벽(아이콘): 원작 아이콘 소스 = 03xxx 장비 .dat의 part1(단일 프레임 착용 미리보기 그림) + 01xxx/07xxx 소품(붕대 01677·진통제 01678·회복팩 01680·주사기 01785·앰플 01756·렌즈 01848·티켓/쿠폰 01686·서류 01692·에너지드링크 07155·탄약박스 07727). 08441~08469는 07xxx에 보라 테두리 씌운 퀘스트 아이콘이라 미사용. 별도 아이콘 세트는 없음(광역 검색 결과 마커 13개뿐). tools/ec-icons.py → public/art/icon_*.png 22장(64×64 투명, 2배 이상은 정수 NEAREST 확대, 축소는 LANCZOS). 탄약 6종은 원작 박스 그림이 1장뿐이라 ammo_normal을 색조만 바꿔 파생(추정 아님, 의도적 착색). icon_ammo_rocket은 원작 소스 없어 절차 그리기 유지, icon_consumable 플레이스홀더 유지. artOverrides 22줄 추가. tsc·vitest 225·build 통과, e2e/out/ui_icons.png(인벤토리 행+퀵슬롯+상태창) 확인. 후보 시트 라벨이 그림 아래에 있는 걸 위로 잘못 읽어 1차 매핑이 틀렸었음 → 재매핑. 다음: 효과음(s0/s1 658 WAV, city.exe에 이름표 없음 → 매핑 방법 결정).
- 2026-09-17 새벽(효과음·배포): 효과음 이름표는 클라이언트 어디에도 없음(city.exe 문자열·.dat/.dta·Local.dat·.fog 전부 확인) → 추측 배정 대신 (1) 게임에 원작 WAV 덮어쓰기 훅 추가: artOverrides.ts `SFX_OVERRIDES`(이름 또는 'shot:무기종류' → 파일 URL, 지금은 비어 있어 절차 합성음 그대로), AudioManager가 시작 시 fetch+decode해 samples에 보관하고 play()/gunshot()이 샘플 우선 재생; tools/ec-sounds.py가 SFX 표대로 D:의 .dat를 public/art/sfx/*.wav로 복사(표 비어 있음). (2) 사용자가 귀로 골라 배정하는 '효과음 고르기' 페이지(scratchpad/sfx-picker: 656개 WAV를 22050Hz 모노로 정규화해 sfx_sprite.mp3 7.9MB 한 장 + 구간표, db 문서 sfx/map에 저장) 만들었으나 **아직 게시 안 함**(사용자가 전체화면 문제로 중단). 전체화면: 아티팩트 뷰어는 iframe이라 불가 → GitHub Pages로 공개 배포 완료, 플레이 주소 https://tpwnsdk1995-afk.github.io/eternal-city/ (상세는 _인프라-현황.md '공개 배포'). 저장소 public 전환은 사용자 지시. 다음: 효과음 고르기 페이지 게시 → 사용자 배정 결과(ArtifactData sfx/map)를 SFX_OVERRIDES·ec-sounds.py에 반영 → 아티팩트 v34.
- 2026-09-17 새벽(효과음 배정·휴대폰 전체화면, b0a6288): 사용자가 656개를 귀로 고르는 건 거부("알아서 해") → 고르기 페이지는 폐기. scratchpad/sfx-feat.py로 656개 WAV의 파형 특징(길이·피크·어택·감쇠·온셋 수·스펙트럼 중심·저/고역 비율·평탄도·지속)을 뽑아 37개 자리에 자동 배정 (s0359~0383=총성 계열, s0210/0211/0217=징글, s0214=동전, s0223=클릭, s0290~0293=UI 톤, s1xxx=목소리/괴물). 확신도: 총성 높음, 징글/UI 중간, 피격/괴물 낮음 → 사용자가 이상하다고 한 소리만 id 바꾸면 됨. tools/ec-sounds.py SFX 표 37줄 + artOverrides SFX_OVERRIDES 37줄 + public/art/sfx/*.wav 37개(1.4MB). 헤드리스 Edge에서 samples=37 디코드·404 없음 확인. 휴대폰 전체화면 안 되던 원인: 터치 시작(pointerdown)은 브라우저가 "사용자 동작"으로 안 쳐서 requestFullscreen 거부 → TouchControls 탭·타이틀 ⛶ 버튼을 pointerup(손 뗄 때)으로 바꿈, 전체화면 진입 시 가로 고정(screen.orientation.lock, 미지원 브라우저는 무시). 타이틀 버튼 글자 13→16. 배포 후 라이브에서 art/sfx/shot_권총.wav 200 확인. **휴대폰 실기 테스트는 사용자 대기.** 다음: 사용자 피드백 반영(전체화면 실기·이상한 효과음 id 교체) → 아티팩트 v34 → M15.
- 2026-09-17 새벽(전체화면 정렬): 휴대폰 전체화면에서 게임이 왼쪽에 붙고 오른쪽만 검게 비던 원인 = autoCenter NO_CENTER라 정렬을 #game div의 flex에 맡기는데, Phaser가 전체화면 시 캔버스를 자기 wrapper div로 감싸 그 div를 전체화면으로 띄워 flex 정렬이 사라짐 → gameConfig scale에 fullscreenTarget:'game' 한 줄. 2000×818 뷰포트에서 양옆 273px 동일 확인. 2.4:1 휴대폰에선 양옆 검은 띠 자체는 남음(게임이 16:9 고정, GAME_WIDTH 상수 58곳) — 화면 꽉 채우기(Scale.EXPAND + 상수→scale.width 전환)는 별도 작업으로 보류.
- 2026-09-17 새벽(전체화면 재확인, 70fc42b): 사용자가 994509c 뒤에도 "그대로"라며 같은 모양 사진 → 헤드리스 Edge 2000×818에서 F키로 진짜 전체화면(document.fullscreenElement=#game, scale.isFullscreen=true) 진입시켜 캔버스 좌우 273/273 확인. 원인 추정: Pages가 index.html에 Cache-Control max-age=600(10분)을 붙여 휴대폰이 옛 페이지를 보여줌. 대책: 타이틀 맨 아래 줄에 빌드 커밋 표시(vite define __COMMIT__ = GITHUB_SHA 앞 7자, 로컬은 dev) + index.html에 `:fullscreen{display:flex;…}` 보험. 사진에서 "개발 빌드 70fc42b" 이상이 보여야 새 버전.

## 2026-09-17 아침 — 휴대폰 플레이 피드백 6건 조치
사용자 보고: ① 좌우로 걸을 때 캐릭터가 반대를 봄 ② 골드 드랍 너무 적음 ③ 이동속도 조금 느림 ④ 마을 귀환 버튼 ⑤ 아이템 드랍·판매 ⑥ 모바일 UI 너무 작음.
- ① 원인: 플레이어 원본 모델(00046.dat)만 방향 블록이 시계방향(N NE E …)으로 저장돼 있어 동/서·대각선 줄이 거울처럼 뒤집혀 뽑혔음(몬스터는 정상).
  `public/art/player*.png` 3장의 줄을 맞바꿈(0↔4, 1↔3, 5↔7). `tools/ec-extract.py`에 `cw=True` 플래그 추가(플레이어 3항목) → 다시 뽑아도 같은 결과.
- ② `balance.loot.wonMult = 10`, ⑤ `balance.loot.dropChanceMult = 4`(확률 상한 100%) — `rollLoot`에서 곱함. 판매는 원래 상점 창 오른쪽 "판매" 칸(정가 40%).
- ③ `moveSpeedBase` 130 → 150.
- ④ 핫키 `home`: 키보드 H, 터치 상단 "귀환" 탭, Esc 메뉴 "🏠 마을로 귀환" 버튼 → 현재 연도의 마을(hubForYear)로 `travel`. 어설트 장면(`Assault` 활성)에서는 거부 메시지.
- ⑥ `src/config/gameSize.ts`(새 파일, import 없음): 터치 기기(pointer: coarse)면 논리 해상도 높이 540, 너비는 화면 비율에 맞춤(960~1400) → UI 4/3배, 옆 검은 띠 제거. 데스크탑은 1280×720 그대로.
  - `WORLD_ZOOM = 1.5 × (H/720)` 로 월드 시야는 동일. 이 값이 모듈 로드 시 계산되므로 BaseWorldScene은 gameConfig가 아니라 gameSize에서 가져옴(gameConfig↔scene 순환 import로 첫 화면이 안 뜨던 문제).
  - `Window` 기본 클래스: 창 높이 > 화면이면 setScale로 줄이고 y를 위로 당김(contains도 스케일 반영). 메뉴 666·스킬 690 창이 540 화면에 들어감.
  - 휴대폰 배치: 미니맵 왼쪽 위, 퀘스트 추적 오른쪽 위, 상단 탭은 x=240부터(터치 버튼 무리가 옛 미니맵 자리와 겹치던 문제).
  - 타이틀/캐릭터 생성 배경은 화면을 덮도록 확대(cover).
- 검증: tsc 0, vitest 225/225, Edge 휴대폰 흉내(915×412, 터치)로 캔버스 1200×540 확인, 창 4종·타이틀 스크린샷 확인, H 키로 필드→안전지역 귀환 확인, 콘솔 오류 0.

## 2026-09-17 아침 — 휴대폰 피드백 3건 (닫기 버튼·일시정지·달리기 지구력)
- 창 닫기 ✕: 18px 상자 → 26px 상자 + 주변 64px 터치 영역(`src/game/ui/Window.ts`). 모든 창이 이 기반 클래스를 쓰므로 한 곳 수정으로 전부 적용.
- 일시정지: 터치 상단 탭 "⏸ 정지" / 키보드 P. UI 씬이 월드 씬(SafeZone·Field·Assault)을 `scene.pause`, 어두운 덮개 + "화면을 누르면 계속합니다". 덮개 누르면 재개. 일시정지 중 터치 조작판은 꺼짐(탭 이중 반응 방지). 맵 이동·사망으로 씬이 바뀌면 update에서 덮개 자동 해제.
- 달리기 지구력: 소모 코드 제거, `stamina > 0` 조건 제거, 지구력은 달리는 중에도 회복(대기 지연 없음). `balance.stamina.runDrainPerSec`/`regenDelayMs` 값은 남아 있으나 미사용.
- 검증: tsc 0, vitest 225/225, build OK, 휴대폰 에뮬레이션 e2e(탭 정지→탭 재개→P 토글→인벤 닫기 영역 바깥쪽 탭으로 닫힘) 통과. 임시 spec은 삭제.

## 2026-09-17 낮 — 휴대폰 피드백 5건 (글씨 깨짐·장비 상세·경험치·글자 크기·스탯/강화 상승폭)
- 글씨 깨짐: 캔버스를 논리 해상도(폰 540p)로 그린 뒤 화면에 늘려서 흐렸음 → `src/config/gameSize.ts` `RENDER_SCALE`(k, 1~3 배, 0.25 단위 = 화면 짧은 변×dpr ÷ 논리 높이). 캔버스는 논리×k 픽셀, 고정 화면 씬은 `fitCamera(cam)`로 카메라 줌 k, 월드 씬은 `WORLD_ZOOM × k`, 글자 `resolution: k`. 원시 포인터 좌표를 쓰는 곳(InputMapper uiHit, TouchControls)은 `toLogical(v) = v / k`. 캔버스 CSS는 `image-rendering: auto`. 폰 흉내(915×412, dpr 2) → 캔버스 1800×810, k=1.5.
- 장비 상세: 인벤토리 창을 400×640으로 다시 씀. 첫 탭 = 선택 + 아래 상세 패널(공격력→내 스탯 적용값, 연사/사거리/명중, 필요 레벨 부족 시 빨간 ✗, 무게/판매가/부품; 방어구·탄약·소모품·기타도 각자), 같은 줄 다시 탭 또는 패널 버튼 = 장착/사용.
- 경험치: `balance.xp.killMult = 5`(처치 경험치 5배, `xpForKill`).
- 글자 크기: ListView 14/12/13, 상태창(높이 660)·스킬창 라벨/버튼 12~14로 상향.
- 스탯/강화 상승폭: 스탯 1점당 데미지 0.4% → 2% (`derived.meleePerVit`/`rangedPerTech` 0.02, 체력→근접·기술→원거리), 강화 1단계당 6% → 15% (`tuning.enhanceDmgPerLevel` 0.15). 인벤 상세에 "(강화 +N: 공격 +N×15%)" 표시.
- 캐릭터 생성 화면(폰 540 높이): 스탯 줄이 시작/훈련/뒤로 버튼과 겹치고 이름 입력칸이 안 보였음 → `cy(데스크탑, 폰)` 좌표 압축. 이름 입력칸이 어긋난 진짜 원인 = `#game`을 flex 가운데 정렬하면 Phaser DOM 컨테이너(absolute, 캔버스 픽셀 크기 + transform scale)가 캔버스와 다른 위치에 놓임(라이브에서도 (-142,-64) 어긋나 있었음) → `main.ts` `alignDom()`이 컨테이너 left/top을 캔버스 offset에 고정(RESIZE·window resize·첫 프레임), `#game { position: relative }`.
- 터치 e2e: 상단 탭이 7→9개로 늘어난 뒤 `touch.spec`의 하드코딩 좌표가 "상태" 탭을 눌러 실패하고 있었음(기존 결함) → `TouchControls.snapshot().buttons`에 탭 중심 좌표도 포함, 스펙은 `buttons['인벤']` 사용. 달리기 지구력 소모가 없어졌으므로 스펙은 "안 줄어듦"으로 변경.
- 검증: tsc 0, vitest 225/225, build OK, 데스크탑 e2e 8스펙(smoke/touch/controls/windows/combat/tuning/weapons/tutorial) 15/15, 폰 흉내 임시 스펙(캔버스 1800×810·스틱 이동·인벤 탭·행 탭 선택·창 4종·필드) 통과 후 삭제. Playwright 1.56이 찾는 chromium_headless_shell-1194가 없어 `npx playwright install chromium-headless-shell` 1회 실행.
- 남은 것(기존): 스킬창 목록 14행×44px가 폰에서 창 아래 힌트와 겹침. 다음 요청: 캐시템·장비·물약을 원작 목록대로 대량 추가 + 무기 아이콘 개선.

## 2026-09-17 낮 — 아이템 대량 추가 (무기 113·방어구 79·소모품 37) + 아이콘 원작 도감에서 추출
- 사용자: "캐시템·장비·물약 전부 본게임 따라서 많이 만들어놔, 무기 디자인도 별로".
- `src/data/weapons.ts` 다시 씀: 권총 12·기관단총 12·돌격소총 13·산탄총 11·저격소총 12·기관총 9·근접 16·투척중화기 9·불법 5·변이무기 13 = 113종, Lv.1~85 사다리(값 2천~600만). 기존 id는 전부 유지(세이브 호환). `W()` 헬퍼가 `TEX.icon_<id>`로 아이콘을 자동 연결.
- `src/data/armors.ts` 다시 씀: 방어구 79(상의 18·하의 11·코트 16·신발 10·모자 17·가발 7; 세트 = 기동대 Lv30/헌터 협회 Lv40/강남 특무 Lv55/위토 강습 Lv70/서울 특무 Lv85), 소모품 37(음식·음료 11, 의약 9, 캐시 17). `src/data/buffs.ts` 버프 5→11(경화제·아드레날린·신경 자극제·앰플 XL·₩ 쿠폰·무게 스티커).
- 아이콘: 원작 클라이언트 아이콘 도감 파일(00104~00109·00111·00122·00123)의 레이어를 잘라 `public/art/icon_<id>.png` 235장. 매핑표는 `tools/ec-icons.py`의 `ATLAS`(파일, 레이어 번호) — 다시 뽑으려면 `python tools/ec-icons.py`. 옛 손그림 무기 `icon_*.webp` 17장은 삭제.
  - `src/data/textureKeys.ts` icon_ 키 214개 추가, `src/data/artOverrides.ts` png 235줄. `manifest.ts`는 icon_ 키 전부에 기본 그림(drawIconConsumable)을 깔아 두고(`ICON_FALLBACKS`) 실제 파일이 덮어씀 → 파일 하나 빠져도 빌드는 통과.
  - 겹쳤던 5장(지구력 팩=에너지드링크, 체력 알약 중=진통제, 지구력 알약=비타민, 체력 팩 XL=구급상자) 다른 레이어로 교체 확인.
- 상점 진열(`src/data/npcs.ts`)은 표에서 자동 계산: 박상사 = 불법 아닌 무기 전부 + 탄약 전부 + 코스튬 아닌 방어구 전부, 암거래상 = 불법무기 5종 + 철갑/슬러그, 사이버샵 = 캐시 소모품 17 + 코스튬 모자 6·가발 7, 자판기 = 음식·음료·의약 20. 상점 창은 방어구도 무기처럼 "내 레벨+6" 넘는 건 숨김.
- `registry.ts` 검사 추가: 아이콘 없는 아이템이 있으면 시작 시 오류.
- 검증: tsc 0, vitest 225/225(버프 개수 검사 5→11로 갱신), build OK, e2e 전체 57개 중 55 통과 + 2 재실행 통과(death.spec은 파방클 값 5만→12만이라 시험용 ₩ 10만→50만으로 갱신, save-transfer는 재실행 시 통과 = 간헐 실패).

## 2026-09-17 낮 — 상점: 모든 등급을 1레벨부터 진열 + 암거래상을 마을에도 배치
- 사용자: "암거래상 마을에도 두고 / 4등급 이상 무기들은 레벨 오르면 알아서 풀린다는거야? 그냥 처음부터 다 보이게 해 내가 레벨 맞춰서 사게".
- `src/game/ui/ShopWindow.ts`: 레벨 따라 등급 상한(`shopGradeCap`)·"내 레벨+6" 무기/방어구 숨김·탄약 구경 필터(`ammoRelevant`) 전부 삭제. 대신 구매 목록 위에 등급 스테퍼 `◀ N등급 ▶`(1~11, 창 필드 `grade`). 무기는 매장 재고 전부가 그 등급 하나로 한 줄씩(무기 자체 등급 범위를 벗어나면 그 범위로 잘라 표시). 레벨 부족 줄은 회색/빨강(disabled), 돈은 되면 1레벨이라도 11등급 구매 가능. 스테퍼는 무기 파는 상점(박상사·암거래상)에서만 보임.
- `src/data/maps/gwangjin-gucheong-parking.ts`: `npc_blackmarket`을 (4,19)에 추가(사이버샵 단말기 (8,19) 왼쪽). 아차산역 (47,42) 배치는 그대로.
- `e2e/weapons.spec.ts`: 제목만 "shop lists every weapon from level 1; the buyer picks the grade"로 바꿈(본문은 그대로 통과).
- 검증: tsc 0, vitest 225/225, build OK, e2e 10/10(weapons/tuning/cybershop/smoke/windows/death + 임시 스펙: ▶ 10번 눌러 11등급 화면, 1레벨 ₩500만으로 글록17 11등급 구매 OK, 암거래상 창, 마을 (5,19)에서 스크린샷 — NPC 겹침 없음). 임시 스펙 삭제.

## 2026-09-17 저녁 — 무기 등급 삭제 + 매매 확인창 + 불법무기 최종 티어 22종
- 사용자: "무기별로 등급이랑 레벨이 따로 있는게 말이안돼, 등급을 빼버려 전부 / 터치하면 바로 팔리고 사지는데 확인창 한번 뜨게해 / 불법무기는 최종무기야, 스펙 제일 좋고 특이해야지, 비주얼도 멋있어야, 근접·원거리 모두 종류 늘려".
- 등급 삭제: `WeaponDef.gradeMin/gradeMax`, `ItemStack.grade`, `EquippedWeapon.grade`, `gradeMult`, `weaponPrice`, `balance.combat.gradeMult` 전부 제거. 무기는 레벨·공격·가격 하나씩. 강화(+N)는 그대로. 가격 = 표의 `price` 그대로(`buyPrice(def) = def.price`), 판매 = 정가 40%. `weaponLabel`은 "M16A2 +3 [정밀]". 저장 데이터의 옛 `grade` 필드는 무시됨(읽지 않음).
- 확인창: `src/game/ui/ShopWindow.ts` 다시 씀. 구매/판매 줄을 탭하면 목록이 숨고 가운데에 "이름 / ₩N에 구매(판매)할까요?" + 확인/취소 버튼(`pending`). 확인을 눌러야 `actions.buy/sell` 실행. 등급 스테퍼는 삭제.
- 불법무기: 옛 5종(tec9·sawed_off·ak47_mod·deagle_gold·saiga_mod, Lv6~34) 삭제 → 22종 최종 티어(Lv.62~90, ₩320만~1,300만, 전부 특무형보다 강함, 하나씩 특이점): 권총 PHANTOM-9(900rpm)·저지먼트 .50(한 발 480), 기관단총 호넷 SWARM(2400rpm)·바이퍼 PDW, 돌격소총 나이트폴·버서크 AK(1500rpm), 산탄총 히드라(420rpm)·선브레이커(12펠릿), 저격 레이스 DMR(240rpm)·레일건 X-0(한 발 3600), 기관총 아크 캐논·템페스트(1800rpm), 중화기 플라즈마 박격포(반경 140)·헬파이어 다연장, 근접 8종(광자 차크람 300rpm, 처형자 철퇴 1500, 빙정 도끼, 보이드 대부, 진홍 삼지창 사거리 120, 월광검, 천공의 황금창, 단두 대부월 2600). 암거래상은 `illegal` 무기 자동 진열 + .50 철갑/유탄/로켓 탄약 추가.
- 아이콘 22장: 원작 도감의 안 쓰던 SF/판타지 레이어(00111 L32·34·39·43·50, 00123 L6·8·46·47·55·57·58·59·64·82·91·92·93, 00104 L8·9·52·63) → `tools/ec-icons.py` ATLAS, `python tools/ec-icons.py`로 재추출. 옛 5장 png 삭제.
- 세이브 호환: `saveService.applySnapshot`이 등록 안 된 itemId 스택(옛 불법무기)을 인벤·창고에서 조용히 버림.
- e2e: `weapons.spec` 암거래상 시험은 Lv90 ₩3천만으로 PHANTOM-9 구매·장착. 임시 스펙(줄 탭→확인창 뜸→취소하면 안 사짐→확인하면 ₩ 빠지고 인벤 +1, 암거래상 첫 줄 PHANTOM-9 확인창) 통과 후 삭제.

## 2026-09-17 밤 — 보관함 100칸·무게 2배+·탄약 1000발 박스·스탯 확인창·강화 확률 상향
- 사용자: "인벤토리 슬롯 2배 이상 / 무게 제한 2배 이상 / 5.56 같은 건 1000발씩 팔게 / 스탯도 확정할지 한번 더 물어보게 / 강화 확률 대폭 올려, 거의 10강 밑으로는 성공해야지".
- 인벤토리는 원래 코드에 칸 수 제한이 없음(무게만 제한). 칸 제한은 구청 보관함(`balance.storage.slots`)뿐이라 40→100.
- 무게: `balance.weight` base 30→70, perVit/perEnd 0.25→0.5 (Lv1 기본 약 32kg→74kg).
- 탄약(`src/data/ammo.ts`): 박스 크기 9mm/.45/5.56/7.62 일반 1000발, 소이/철갑 500발, 12ga 산탄 200·슬러그 150, .50 철갑 100, 유탄 30, 로켓 12. 가격은 발당 단가 그대로 × 박스 크기(5.56 일반 120발 ₩3,600 → 1000발 ₩30,000). 판매가·정렬은 원래 `qty/boxSize` 비율이라 그대로 맞음.
- 시작 지급(`starterKit.ts`): ₩5,000→₩50,000(탄약 박스 한두 개 살 수 있게), 9mm 일반탄 100→1000발.
- 확인창: `ShopWindow` 전용이던 `pending/confirm`을 `Window` 베이스로 올림(`confirm(text, run)` + `drawConfirm()`). `StatusWindow` +1/+5는 "생명력 +1 / 확정할까요? (되돌릴 수 없습니다)" 확인 후에만 `actions.allocate`.
- 강화 확률 `enhanceSuccessPct`: [50,45,40,35,30,25,20,15,10] → [100,100,100,100,100,95,90,85,80] (+5까지 무조건, +9까지 80% 이상). 실패 시 -1, +0 안전은 그대로.
- 테스트 갱신: tuning/buffs 단위 테스트(새 표 기준), smoke·save-slots·combat(₩50,000 시작), weapons(12ga 200발, 돈 부족 시험은 m24 ₩65,000), launchers(유탄 30발·로켓 12발).
- 검증: tsc 0, vitest 223/223, build OK, e2e 57/57(전체 53 + 위 4개 수정 후 재실행 통과). 임시 스펙(+1 탭→확인창→취소하면 포인트 그대로→확인하면 5→4, 5.56 박스 1000발 ₩30,000, 무게 한도 ≥70kg) 통과 후 삭제.

## 2026-09-17 — 상점 판매 여러 개 체크해서 한 번에
- 사용자: "잡탬 하나하나 파는 게 오래 걸린다, 여러 개 체크해서 한 번에 판매"
- `ShopWindow.ts`만 수정(+35/-8): 판매 줄 탭 = ☐/☑ 토글(picked Set, 인벤토리에서 사라진 uid는 refresh 때 정리, NPC 바뀌면 초기화). 오른쪽 위 버튼 "선택 N개 판매 +₩합계" → 확인창 한 번 → 체크된 것 전부 `actions.sell` 반복. 소모품/잡템은 줄 하나 = 스택 전부(예전 "1개씩 판매" 삭제, 부제 "×N (전부 판매)"). 퀘스트 아이템/값 0 잡템은 체크 불가 그대로.
- 검증: tsc·vitest 223/223·build 통과. 임시 e2e(줄 두 개 실제 클릭 → 버튼 → 확인)에서 경찰봉+12게이지 박스 둘 다 팔리고 +₩7,600(=(3,000+16,000)×40%). 창 좌표: 상점 창 (240,50) 배율 1, 판매 줄 중심 x=837, y=155+i×42.
- 건너뜀: "전체 선택" 버튼 — 필요하면 한 줄(picked에 판매 가능 uid 전부 add).

## 2026-09-17 — 어설트 보상×50·난이도, 버리기, 줍기 범위, 탄약 합치기, 과적 감속, 무게×2, 연도 해금 안내
- 어설트 A~F 클리어 보상: ₩·경험치 ×50, 보상 아이템 개수 ×5 (`src/data/assaults/assault-*.ts`). 실패 벌금은 그대로.
- 난이도: 모든 어설트 몬스터 체력 ×2, 피해 ×1.5 (`balance.assault.hard`, `hardMonster()` in `src/core/assault/advanced.ts`, `AssaultScene` 스폰에서 적용). 고급 어설트(×1.7/×1.4)는 그 위에 곱해짐. e2e는 killAll로 잡으므로 체력 배율 영향 없음.
- 버리기: 인벤토리 → 아이템 탭 → 오른쪽 빨간 `버리기` → 확인창. 퀘스트 아이템은 버튼 없음. `actions.discard(uid)`; 장착 중이면 해제, 탄약이면 일반탄으로 되돌림.
- 줍기 범위: 드롭 몸체 원 반지름 8→28px (`Pickup.ts`). 스쳐 지나가도 먹힘.
- 같은 탄약 박스 합치기: `addItem`이 ammo도 소모품처럼 합침. 단위 테스트 2개를 새 동작에 맞춰 고침(`tests/core/inventory.test.ts`, `fireController.test.ts` — 후자는 옛 세이브의 박스 두 개를 직접 만들어 연쇄 소비를 계속 검증).
- 과적: 이제 줍기를 막지 않고 경고만("무게 초과 — 이동이 느려집니다"). 감속은 원래 있던 `Player.ts` overweightMult 0.6.
- 무게 한도 ×2: base 140, 체력/지구력당 1kg (이전 70/0.5/0.5).
- 연도 해금 안내: 패러렐 창 잠긴 줄 오른쪽에 `허가증 → 김훈 소대장 퀘스트 "위토군 배치문서 회수"` 또는 `Lv.N 또는 YYYY 캠페인 완주 필요`. HUD 연도 옆도 `허가증 없음 → 김훈 소대장 퀘스트`.
- 검증: tsc·vitest 223/223·build 통과, 대상 e2e 10/10(임시 zz-discard: 탄약 1묶음 2000발, 버리기→확인으로 경찰봉 삭제, 최대 무게 ≥140), 전체 e2e는 아래 커밋 항목 참조.

## 2026-09-17 — 스탯 일괄투자 (전부 버튼)
- `StatusWindow.ts`: 각 스탯 줄에 `+1` `+5` 옆 `전부` 버튼. 남은 포인트와 상한까지 남은 칸 중 작은 값만큼 확인창 한 번 후 `actions.allocate`. 버튼은 폭 따라 이어 붙임(x=272부터, 13px).
- 검증: tsc·vitest·build 통과, windows e2e + 임시 e2e(체력 전부 → 포인트 5→0, 체력 +5) 통과 후 임시 스펙 삭제. 스크린샷으로 창 폭 380 안에 들어감 확인.

## 2026-09-17 — 소모품 수량 입력 구매
- `ShopWindow.ts`: 소모품 줄을 누르면 목록 대신 수량 화면(`qtyBuy`). `-10 -1 +1 +10 최대 직접 입력` + `구매/취소`. 최대 = 지금 돈으로 살 수 있는 개수(999 상한). `직접 입력`은 브라우저 기본 입력창(window.prompt). 무기·방어구·탄약은 예전처럼 확인창.
- `actions.buyMany(itemId, n)`: 한 번에 n개 구매, 돈·무게 한도에서 멈추고 못 산 개수를 메시지로 알림(메시지 1줄).
- 검증: tsc·vitest·build 통과, 상점 관련 e2e 8개 + 임시 e2e(자판기 콜라 +10 +1 → 12개, ₩3,600 차감) 통과 후 임시 스펙 삭제.

## 2026-09-17 — 어설트: 미리 부순 기물로 멈추는 버그, 귀환으로 포기
- 사용자: "환기구 미리 깼더니 아무 반응이 없네 어설트가 안끝나"
- 원인: `assaultMachine.ts` enterPhase가 destroy 단계에 들어갈 때 `objectivesLeft`를 목록 전체로 새로 채움 → 앞 단계에서 이미 부순 기물은 다시 부서질 수 없어 영원히 남음.
- 수정: 런타임에 `destroyed: string[]` 추가, `onObjectiveDestroyed`가 기록, enterPhase는 이미 부순 것을 빼고 시작(전부 부쉈으면 바로 다음 단계). 단위 테스트 추가(`tests/core/assault.test.ts`).
- 포기: 어설트 중 귀환(H 키·터치 "귀환" 탭·메뉴 귀환 버튼) 1번 → "한 번 더 누르면 포기" 안내, 4초 안에 한 번 더 → `assaultRetreat` 이벤트 → AssaultScene이 실패(`retreat`, "포기") 처리, 실패 벌금 차감, 3초 후 마을로. 결과창 문구 "작전을 포기하고 귀환했습니다."
- 검증: tsc·vitest 224/224·build 통과, 어설트 A·E·F e2e + 임시 e2e(H 두 번 → 결과창·벌금 ₩500·마을 이동) 통과 후 임시 스펙 삭제.

## 2026-09-17 — 무게 제한·어설트 레벨 제한 제거
- 사용자 지시 "무게 제한 없애고 어설트 레벨제한 없애라".
- 무게: 구매·일괄구매·보관함 꺼내기 거부, 과적 감속, 줍기 경고 전부 삭제. 창에는 현재 kg만 표시(최대치·⚠ 과적 삭제). derived.maxWeightKg 값 자체는 쿠폰/길드 테스트 때문에 남겨둠(쓰는 곳 없음).
- 어설트: balance.assault.entryLevelOverride = 0 → 모든 작전 Lv.1부터 지원 가능. 목록의 "Lv.N+"와 "Lv.N 필요" 버튼 삭제, 상세에 권장 레벨만 남김.
- 확인: tsc·vitest 224·build, e2e storage/assault-c/cybershop/guild/smoke 9개 통과.

## 2026-09-17 — 극한 어설트 X 추가
- 사용자 지시 "더 난이도 높고 보상 높은 어설트 하나만 빨리 만들어봐".
- src/data/assaults/assault-x.ts "코엑스 지하 — 최후의 봉쇄 (극한)". E와 같은 맵을 쓰고 웨이브는 약 두 배, advanced(고급 몬스터 보정: 체력 ×1.7, 피해 ×1.4) 적용.
- 보스 2연속: 패러사이트 뿌리 → 위토 사령관. 보상 ₩1억 · XP 1,500만 · .50 AP탄 1000 · 전술 상의 3 · 프리미엄 쿠폰 10(모두 확정). 실패 벌금 ₩20만.
- registry ASSAULTS 끝에 추가(고급 변형은 만들지 않음), 어설트 접수 NPC 목록 끝에 추가.
- e2e assault-ef.spec.ts에 X 끝까지 클리어 테스트 추가. 첫 보스 처치 직후 같은 폴링에서 killAll이 막 나온 2번째 보스까지 죽이던 테스트 순서 문제를 고침.
- 확인: tsc·vitest 224·build, e2e assault-c/assault-ef 5개 통과.

## 2026-09-17 — Ω(종말형) 장비 + 극한 II~IV 어설트
- 무기 9종(클래스마다 1, Lv.95, 암거래상 판매·극한 보상) `omega_*`, 방어구 7종(상의·하의·코트·신발·모자 + CL 상의·코트, 일반 상점) `armor_*_omega*`. 아이콘은 가장 가까운 기존 장비 그림을 빌려 씀(WI 헬퍼, iconTex 덮어쓰기).
- 어설트에 `power` 필드 추가: 고급 보정 위에 몬스터 체력·공격력을 한 번 더 곱함(hardMonster 두 번째 인자).
- `assault-x-plus.ts`: 극한 II 광화문(power 1.5, 보스 사령관→회장좀비, ₩2.5억), 극한 III 코엑스(power 2, 보스 3, ₩5억), 극한 IV 코엑스(power 3, 보스 4, ₩10억, Ω 무기·CL 방어구 확정).
- e2e assault-ef에 극한 II·IV 클리어 테스트 추가(통과). 실제 사람이 깰 수 있는 난이도인지는 미검증.

## 2026-09-17 — 어설트 접수원 전 년도 · 버튼 크게 · 전 품목 수량 구매 · 강화 +20 · 유니크 대폭
- 2006·2008·2017 허브(makeHub)에 어설트 접수원 추가 → 모든 년도 허브에 있음 (parallel.test가 년도별 확인)
- 창 버튼 글자 +4px·여백 확대, 터치 버튼(발사 66/일반 38)·상단 탭(74x38) 확대. 기술상 창 줄 간격을 넓히고 개조 시도 버튼은 제목 줄로 올림(겹침 해결)
- 상점: 장비·탄약 포함 모든 품목이 수량 선택창(buyMany)으로 열림
- 무기 강화 최대 +9 → +20, 성공률 100…80 이후 5%씩 내려 +19→+20 25%. 방어구 플러스업(+5)은 그대로
- 유니크: 정밀 공+50%·치명+25%, 파괴 공+120%, 신속 공+50%·연사+40%, 장거리 공+50%·사거리+40%
