# 인프라 현황 — 이터널시티

## RC 원격 접속 (2026-09-16 설정)
- 방 이름: **이터널시티 (데스크탑)** / 세션 `7131f7e3-8af2-4697-9fbf-7a3913c58685` / cwd `C:\eternal-city-main` / group `이터널시티`
- 등록처: `C:\rcwatch\rc-map.json` rooms 9번째 → 부팅 자동시작 + RC-Watchdog(5분마다 죽은 방 복구) 자동 적용
- rc-map 백업: `C:\rcwatch\backup\rc-map.json.bak-20260916`
- 앱 세션(local_acab679e…)에도 RC 스위치를 켰음.
- ⚠️ 워치독이 띄우는 CLI 방과 데스크탑 앱에서 연 이 세션이 **같은 .jsonl에 동시에 쓰면 안 된다.**
  워치독 방이 뜬 뒤에는 claude.ai의 'RC 방'으로만 열 것 (네이버 카페 방 2026-09-08 선례).

### 2026-09-16 20:04~20:10 진행
- 20:04 워치독이 방 기동(PID 16352). 프로세스·네트워크는 살았으나 **.jsonl 기록 0, claude.ai 항목 생성 0** → 멈춘 상태로 판단.
  원인은 앱 세션이 같은 대화를 쓰는 중이었기 때문으로 추정(미확정).
- 조치: 16352 종료 → 앱 세션 RC 끔(웹 항목 즉시 사라짐, 겹침 없음) → 앱 세션 보관(닫음).
- 다음 워치독 주기(5분 내)에 방이 새로 떠야 함. **미검증.** 다음 세션은 watch-log의 `revive: 이터널시티` 이후
  이 .jsonl 꼬리에 `bridge_status` 줄이 생겼는지, claude.ai에 `이터널시티 (데스크탑)` 항목이 있는지 확인하고
  그룹 `이터널시티`로 옮길 것.

### ✅ 2026-09-16 RC 방 정상 기동 확인
- 워치독 재기동 후 RC 방 연결 확인. 사용자가 원격으로 메시지 왕복함.
- claude.ai 세션: `session_01TE2JjM5LkbkKvXeaEtJWKL`
- 사이드바 정리는 사용자 지시로 **하지 않음** (필요 없다고 함).

## Git / 자동 재개 (2026-09-16 밤)
- 폴더는 원래 git 저장소가 아니었음 → `git init` + origin `https://github.com/tpwnsdk1995-afk/eternal-city`, `main`을 origin/main(7c58b2f)에 맞춤. 작업 트리는 원격과 동일했음(추가 파일: 작업기록/, .claude/ 만).
- 작성자: 로컬 설정 `caca1 / tpwnsdk1995@gmail.com`. credential.helper = manager(Git Credential Manager).
- 2시간마다 자동 재개 크론(세션 내 job 621904cf, 매 짝수시 :23, 7일 후 만료). **세션 전용** — RC 방이 워치독으로 재기동되면 사라지므로 다시 걸어야 함.
- 작업 순서·진행 상태: `작업기록/_다음에-이어서.md`.

## 공개 배포 — GitHub Pages (2026-09-17 새벽)
- **플레이 주소: https://tpwnsdk1995-afk.github.io/eternal-city/** (브라우저 새 탭에서 열리므로 전체화면(F) 버튼이 실제로 동작. 아티팩트 뷰어 안에서는 iframe이라 불가)
- 저장소 `tpwnsdk1995-afk/eternal-city`는 **2026-09-17 사용자 지시("1번해", "니가해")로 public 전환**. 이유: 무료 플랜은 public 저장소만 Pages 가능(422 "Your current plan does not support GitHub Pages"). 전환은 API PATCH `private:false` (auto mode가 curl을 막아 python 스크립트로 실행, 스크립트는 삭제).
- Pages 사이트 생성: API `POST /repos/.../pages {"build_type":"workflow"}` (HTTP 201). 워크플로의 자동 토큰(GITHUB_TOKEN)으로는 "Resource not accessible by integration"이라 사이트 생성 불가 → 사용자 토큰으로 1회 생성해 둠. 이후엔 필요 없음.
- 배포 방식: `.github/workflows/pages.yml` — main에 push할 때마다 `npm ci` → `npm run build` → dist/ 업로드 → 배포(약 1분). 첫 실행은 YAML 오류(environment를 한 줄 `{ }`로 쓰면 `${{ }}`의 `}}`가 충돌 → 작업 0개로 실패), 77b9cd7에서 블록 표기로 수정.
- vite `base: './'` (9e61308): 자산 경로가 상대라 dist/가 루트(/)·/eternal-city/·아티팩트 어디서든 동작.
- GitHub 토큰은 Git Credential Manager에서 실행 시점에 꺼냄: `printf 'protocol=https
host=github.com
' | git credential fill` (scopes gist, repo, workflow). 값은 절대 출력하지 않음.
- 확인(2026-09-17 새벽): run 35148654178 success, 사이트 HTTP 200, index.js 2.0MB·art/*.webp 200, Edge 헤드리스에서 canvas 생성·오류 0, 타이틀 화면 스크린샷 확인.
